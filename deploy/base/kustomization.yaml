apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - secretstore.yaml
  - externalsecret.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/name: plane-tracker
  app.kubernetes.io/part-of: plane-tracker

# CI rewrites `newTag` (see .github/workflows/build-push.yml). The placeholder
# `plane-tracker` image name is replaced with your ECR repo here.
images:
  - name: plane-tracker
    newName: 426043895157.dkr.ecr.us-east-1.amazonaws.com/plane-tracker
    newTag: latest
